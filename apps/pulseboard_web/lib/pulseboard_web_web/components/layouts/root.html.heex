<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{assigns[:page_title] || "Pulseboard"}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <body class="min-h-screen bg-[#111827] text-white font-sans antialiased">
    <header class="w-full px-6 py-4 bg-[#0f172a] border-b border-gray-700 flex items-center justify-between shadow-sm">
      <h1 class="text-xl font-bold tracking-tight text-white">Pulseboard</h1>
      <nav class="text-sm space-x-6 text-gray-400 flex items-center">
        <a href="#" class="hover:text-white transition">Docs</a>
        <a href="#" class="hover:text-white transition">Support</a>
        <a href="#" class="hover:text-white transition">GitHub</a>

        <%= if assigns[:current_user] do %>
          <span class="ml-4 text-sm text-green-400">Hello, {assigns[:current_user].email}</span>

          <form action={~p"/logout"} method="post" class="inline ml-4">
            <input type="hidden" name="_method" value="delete" />
            <input type="hidden" name="_csrf_token" value={get_csrf_token()} />
            <button type="submit" class="text-sm hover:text-red-400 transition">
              Logout
            </button>
          </form>
        <% end %>
      </nav>
    </header>

    <main class="px-4 py-8">
      {@inner_content}
    </main>

    <footer class="bg-[#0f172a] text-gray-400 text-sm mt-12 px-6 py-8">
      <div class="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0">
        <div class="text-center md:text-left">
          © {Date.utc_today().year} Pulseboard. All rights reserved.
        </div>

        <div class="flex space-x-4">
          <a href="#" class="hover:text-white transition">Privacy Policy</a>
          <a href="#" class="hover:text-white transition">Terms of Service</a>
          <a href="#" class="hover:text-white transition">Contact</a>
        </div>
      </div>
    </footer>
  </body>
</html>
